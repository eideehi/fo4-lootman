# Native Hook Tools

Native-hook address maintenance lives here.

Root commands:

```bash
pnpm run native-hooks:generate
pnpm run native-hooks:bundle
pnpm run native-hooks:verify
pnpm run native-hooks:resolve
pnpm run test:tools
```

See `docs/workflows/native-hooks.md`.
